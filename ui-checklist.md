# tw-weather UI Review Checklist

本文件是 UI 驗收、自查、PR review 使用的快速清單。

- `uiux.md`：完整規範來源（source of truth）
- `ui-checklist.md`：高頻驗收版，不負責定義新規則

若 checklist 與 `uiux.md` 有衝突，**以 `uiux.md` 為準**。

## AI Usage Contract

本清單主要提供 AI assistant、review agent、PR reviewer 作為最終輸出前的驗收規則。

### 使用方式

1. 先依 `uiux.md` 實作或提出方案
2. 產出前逐條檢查本清單
3. 任一高優先項不通過時，應先修正，不可直接交付

### 判定原則

- 勾選代表：已明確滿足或可由輸出內容直接驗證
- 不可勾選代表：沒有證據、與規範衝突、或需要靠猜測才成立
- 若遇到風格與可用性衝突，優先可讀性、資訊層級、觸控與無障礙

---

## 1. 視覺方向

- [ ] 畫面看起來像工具型天氣 app，而不是展示頁、行銷頁或炫技作品
- [ ] 畫面具備 modern / minimal / glass 的一致材質語言，而不是只有普通平面工具感
- [ ] 同一畫面沒有超過一個高權重視覺焦點；若有，判定不通過
- [ ] 沒有高飽和大面積背景、誇張漸層、擬物天氣主視覺；若存在其一，判定不通過
- [ ] 裝飾層沒有壓過主資訊；若使用者第一眼先看到裝飾而不是答案，判定不通過

---

## 2. 文字清晰度

- [ ] 正文文字與背景對比至少 `4.5:1`
- [ ] 大字 / 粗體大標對比至少 `3:1`
- [ ] 核心天氣數值、主要操作文字沒有使用 `textSecondary` / `textTertiary`；若有，判定不通過
- [ ] `12px` 只用於 caption / metadata，不承載重要資訊；若承載主要資訊，判定不通過
- [ ] 正文預設使用 `15` 或 `16`；若大量正文落到 `14` 或以下，需有明確理由
- [ ] 沒有淺底白字、低對比灰字、靠陰影硬撐可讀性的情況；若有，判定不通過

---

## 3. Glass / 半透明

- [ ] 毛玻璃 / 半透明只用在適合的場景：header、tab bar、sheet、次層裝飾
- [ ] 畫面有合理使用 glass 作為材質語言，而不是完全沒有玻璃感，或到處都是玻璃感
- [ ] 沒有把 glass 用在長文區、主要表單區、大量列表 row 背景；若有，判定不通過
- [ ] blur 強度沒有超過規範上限；若超過，判定不通過
- [ ] 半透明 surface 上的正文已驗證可讀性；若無法證明，視為不通過
- [ ] 若可讀性不足，已改用實色 surface；若仍保留 glass，判定不通過
- [ ] glass 是層次輔助，不是畫面主角；若 glass 成為第一視覺焦點，判定不通過
- [ ] glass 已形成一致的材質感：border、alpha、blur、亮部處理一致；若每塊玻璃風格不同，判定不通過

---

## 4. 動畫與互動

- [ ] 該動的地方有動：route、sheet、tab 切換、pressed / selected state；若完全沒有狀態回饋，需補足
- [ ] 動畫是功能性的，不是裝飾性的；若無法說明動畫在表達什麼，判定不通過
- [ ] 沒有持續漂浮、發光流動、無意義粒子效果；若存在，判定不通過
- [ ] micro interaction 約 `160–220ms`
- [ ] route / modal 轉場約 `220–320ms`
- [ ] 動畫只使用 `opacity` / `transform`；若依賴 `width` / `height` / `top` / `left`，判定不通過
- [ ] 動畫沒有造成 layout shift；若造成內容跳動，判定不通過
- [ ] 按壓回饋在 `100ms` 內可見；若按了像沒按，判定不通過
- [ ] 支援 reduced motion；若無降級方案，判定不通過

---

## 5. 資訊架構

- [ ] 每個區塊最多三層：Primary / Secondary / Meta；若超過三層，需拆區塊
- [ ] Primary 內容放在最先被掃到的位置；若主答案需要多次掃描才找到，判定不通過
- [ ] Meta 沒有搶過 Secondary；若更新時間、資料來源比主資訊更醒目，判定不通過
- [ ] 沒有把太多同權重資訊塞在同一畫面；若多個區塊都在爭主視覺，判定不通過
- [ ] 畫面排序符合使用頻率、理解難度、影響範圍

---

## 6. 文案與說明文字

- [ ] 能靠 UI 狀態表達的內容，沒有多寫說明文字；若文案只是在重述狀態，判定不通過
- [ ] `description` 只在 title 不足以解釋時出現；若只是改寫 title，刪除
- [ ] `hint` 只用在風險、限制、資料影響；若不影響決策，判定不通過
- [ ] `footer note` 只保留真正重要提醒；若只是二次解釋，判定不通過
- [ ] 沒有用說明文重複 UI 已經表達清楚的事
- [ ] 沒有無決策價值的背景說明
- [ ] 文案短句、直接、非 marketing 語氣

---

## 7. Card / List / Settings

### Card

- [ ] 一眼能看懂這張卡在回答什麼問題；若卡片用途不明，判定不通過
- [ ] 第一層資訊 1 秒內可掃到；若主數值被裝飾、標籤或次資訊稀釋，判定不通過
- [ ] 卡片內最多只有 1 個強調色；若多個顏色同時爭焦點，判定不通過
- [ ] icon 只輔助辨識，沒有搶主數值

### List

- [ ] 每列高度至少 `56–72`
- [ ] 欄位節奏一致；若每列排版規則不同，判定不通過
- [ ] 數值欄位適合右對齊時已對齊
- [ ] 長列表沒有使用干擾掃描的 stagger 動畫；若長列表逐列炫技進場，判定不通過

### Settings

- [ ] 預設極簡，不是每列都有 description / hint；若每列都像文件說明，判定不通過
- [ ] switch row 整列可點；若只能點 switch 小區塊，判定不通過
- [ ] destructive item 已單獨分組
- [ ] `selected` / `disabled` / `error` 都同時有語意與視覺差異；若只靠顏色，判定不通過

---

## 8. Tab Bar / Navigation

- [ ] top-level tabs 沒有超過 `5` 個；若超過，判定不通過
- [ ] 每個 tab 都有 icon + label；若只剩 icon，判定不通過
- [ ] active / inactive 狀態清楚且不低對比
- [ ] tab bar 有保留 safe area
- [ ] 底部內容沒有被 tab bar 擋住；若最後一筆內容被遮住，判定不通過

---

## 9. Accessibility / Touch

- [ ] icon-only 元件有 `accessibilityLabel`；若沒有，判定不通過
- [ ] 狀態不是只靠顏色表達；若 selected / error / disabled 只能靠顏色辨識，判定不通過
- [ ] 字體放大後仍可讀；若放大後截斷或重疊嚴重，判定不通過
- [ ] 最小可點區域至少 `44x44`；若低於此值，判定不通過
- [ ] 相鄰可點元素至少有 `8` 間距；若容易誤觸，判定不通過
- [ ] 不依賴 hover
- [ ] dark mode 與 reduced motion 都可用；若任一模式缺失，判定不通過

---

## 10. NativeWind / Token 使用

- [ ] 元件內沒有直接寫 raw hex；若直接寫入視覺色值，判定不通過
- [ ] 元件內沒有直接寫 `bg-blue-500`、`text-slate-700` 這類視覺值；若有，判定不通過
- [ ] 有使用 semantic tokens
- [ ] class 與 token 命名一致；若 token 與 class 語意不一致，判定不通過

---

## 11. Anti-pattern 快速排查

- [ ] 沒有過強玻璃感 / 過多陰影；若畫面層次主要靠特效撐起，判定不通過
- [ ] 沒有彩色大片背景；若背景搶過內容，判定不通過
- [ ] 沒有過度動畫；若同頁存在多種吸睛動效，判定不通過
- [ ] 沒有低對比半透明文字區；若正文落在不穩定背景上，判定不通過
- [ ] 沒有炫光、鏡面反射、流動光帶等展示型玻璃特效；若存在，判定不通過
- [ ] 沒有每個設定都塞滿說明文字；若設定頁像說明書，判定不通過
- [ ] 沒有刪掉文案後還保留大面積空白；若密度未回收，判定不通過

---

## 12. PR 前最後確認

- [ ] 視覺氣質一致：現代、簡約、有質感、帶合理玻璃感，且不像展示頁
- [ ] 文字清晰度合格
- [ ] glass 強度合理，且存在感足以形成材質語言
- [ ] 動畫有做但不花俏
- [ ] 資訊簡潔，說明文字只留必要內容
- [ ] 小螢幕與 dark mode 已檢查；若未檢查，視為未完成
